# Static analysis configuration.
#
# Rules below encode the team's coding standard so it is enforced by the
# analyzer instead of by review comments. `flutter analyze` must report
# zero issues before any PR is opened.
include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Treat these as build-breaking rather than advisory.
    invalid_annotation_target: ignore
    missing_required_param: error
    missing_return: error
    todo: ignore
  exclude:
    # Generated by `flutter gen-l10n`; not hand-written, not linted.
    - lib/l10n/app_localizations*.dart

linter:
  rules:
    # --- Correctness -------------------------------------------------
    always_declare_return_types: true
    avoid_dynamic_calls: true
    avoid_slow_async_io: true
    cancel_subscriptions: true
    close_sinks: true
    unawaited_futures: true
    use_build_context_synchronously: true
    throw_in_finally: true

    # --- Team standard -----------------------------------------------
    avoid_print: true                     # debugPrint (guarded) only
    prefer_single_quotes: true
    require_trailing_commas: true
    directives_ordering: true
    unnecessary_library_name: true

    # --- Performance -------------------------------------------------
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_locals: true
    prefer_final_in_for_each: true

    # --- Readability -------------------------------------------------
    avoid_redundant_argument_values: true
    avoid_unnecessary_containers: true
    prefer_if_null_operators: true
    sized_box_for_whitespace: true
    unnecessary_parenthesis: true
    use_named_constants: true
    use_super_parameters: true
